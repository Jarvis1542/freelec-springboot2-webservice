API를 만들기 위한 클래스
DTO // Controller // Service

================================================================================
Service는 트랜잭션, 도메인 간 순서 보장의 역할만함 (비지니스 로직 처리 X)

web Layer
- 뷰 템플릿 영역
- 외부 요청과 읍답

service Layer
- Controller와 Dao의 중간영역
- @Transactional 사용하는 영역

Repository Layer
- DB 접근하는 영역(DAO와 비슷)

Dto
- 계층 간 데이터 교환을 위한 객체

Domain Model
- @Entity 영역이 도메인이다
- VO 값도 이영역에 해당

================================================================================
그래서 비지니스 영역 처리는 도메인이다.